import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  root: {
    position: "relative"
  },
  cart: {
    width: 40,
    height: 40,
    cursor: "pointer"
  },
  cartBadge: {}
}));

export default styles;
