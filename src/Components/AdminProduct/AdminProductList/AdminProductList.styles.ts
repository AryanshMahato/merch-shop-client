import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  root: {
    background: "white",
    width: "90%",
    maxWidth: 1100,
    display: "flex",
    flexFlow: "column",
    alignItems: "center",
    padding: "2rem",
  },
}));

export default styles;
