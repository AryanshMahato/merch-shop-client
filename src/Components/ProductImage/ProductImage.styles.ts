import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  root: {
    width: "90vw",
    maxWidth: 500,
    maxHeight: 500,
    marginBottom: "4rem",
  },
  image: {
    width: "100%",
  },
}));

export default styles;
