import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  root: {
    width: "90vw",
    maxWidth: 500,
    maxHeight: 500
  },
  image: {
    width: "100%"
  }
}));

export default styles;
