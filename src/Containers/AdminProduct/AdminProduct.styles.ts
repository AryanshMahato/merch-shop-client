import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  root: {
    display: "flex",
    flexFlow: "column",
    alignItems: "center",
    marginBottom: "2rem",
  },
  annotation: {
    width: "100%",
  },
}));

export default styles;
