import { makeStyles } from "@material-ui/core";

const styles = makeStyles(() => ({
  root: {
    width: "100%",
    display: "flex",
    flexFlow: "column",
    alignItems: "center",
  },
  annotation: {
    width: "100%",
  },
}));

export default styles;
